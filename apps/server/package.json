{
  "name": "@vigia/server-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Headless Vigia: the CLI and Docker image for a VPS or a terminal.",
  "scripts": {
    "start": "tsx src/cli.ts",
    "showcase": "tsx scripts/showcase.ts"
  },
  "dependencies": {
    "@vigia/core": "workspace:*",
    "@vigia/engine": "workspace:*",
    "@vigia/server": "workspace:*",
    "@vigia/twitch": "workspace:*"
  }
}
